<ResourceDictionary 
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
    xmlns:local="using:Ian.Controls">
    <!--Style TargetType="local:ListSelector">
        <Style.Setters>
                <Setter Property="ItemTemplate">
                <Setter.Value>
                    <Setter.DataTemplate>
                        <ContentPresenter Margin="5,5,5,5" HorizontalAlignment="Left" 
                                          VerticalAlignment="Bottom" FontFamily="Segoe WP" FontSize="50" 
                                          FontWeight="SemiBold">
                            <ContentPresenter.Content>
                                <Binding Mode="OneWay" />
                            </ContentPresenter.Content>
                        </ContentPresenter>
                    </Setter.DataTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="local:ListSelector">
                        <Grid>
                            <Border Background="{TemplateBinding Background}" 
                                    BorderBrush="{TemplateBinding BorderBrush}" 
                                    BorderThickness="{TemplateBinding BorderThickness}">
                                <ScrollViewer Name="PART_ScrollViewer" Opacity="0" VerticalScrollBarVisibility="Hidden">
                                    <ItemsControl Name="PART_ItemsControl" />
                                </ScrollViewer>
                            </Border>
                            <local:ListSelectorItem x:Name="PART_InactiveStateItem" HorizontalAlignment="Center" VerticalAlignment="Center" IsHitTestVisible="True" IsSelected="False" Opacity="1" Width="{TemplateBinding ItemWidth}" Height="{TemplateBinding ItemHeight}" Margin="{TemplateBinding ItemMargin}" />
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="HorizontalAlignment" Value="Center" />
            <Setter Property="ItemMargin" Value="0,5" />
        </Style.Setters>
    </Style-->
    <!--Style TargetType="local:ListSelectorItem">
        <Setters>
            <SetterBaseCollection>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="local:ListSelectorItem">
                            <Grid>
                                <VisualStateManager.VisualStateGroups>
                                    <VisualStateGroup x:Name="Common">
                                        <VisualState x:Name="Normal" />
                                        <VisualState x:Name="Selected">
                                            <Storyboard>
                                                <DoubleAnimation Duration="0" Storyboard.TargetName="AccentBackgroundBorder" Storyboard.TargetProperty="Opacity" To="1" />
                                                <ColorAnimation Duration="0" Storyboard.TargetName="ContentBrush" Storyboard.TargetProperty="Color" To="#FFFFFFFF" />
                                            </Storyboard>
                                        </VisualState>
                                    </VisualStateGroup>
                                </VisualStateManager.VisualStateGroups>
                                <Border x:Name="BackgroundBorder" Background="Transparent" BorderThickness="3" BorderBrush="{StaticResource PhoneDisabledBrush}" />
                                <Border x:Name="AccentBackgroundBorder" Opacity="0" Background="{StaticResource PhoneAccentBrush}" />
                                <ContentPresenter x:Name="ContentPresenter" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Content="{TemplateBinding ItemContent}" ContentTemplate="{TemplateBinding ItemTemplate}">
                                    <ContentPresenter.Foreground>
                                        <SolidColorBrush x:Name="ContentBrush" Color="{StaticResource PhoneDisabledColor}" />
                                    </ContentPresenter.Foreground>
                                </ContentPresenter>
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </SetterBaseCollection>
        </Setters>
    </Style-->
</ResourceDictionary>

